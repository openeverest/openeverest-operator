apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 10
commands:
  - command: kubectl patch db/test-psmdb-cluster -n $NAMESPACE -p '{"spec":{"engine":{"version":"${PSMDB_DB_ENGINE_VERSION}"}}}' --type merge
