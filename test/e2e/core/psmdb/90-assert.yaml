apiVersion: kuttl.dev/v1
kind: TestAssert
timeout: 30
commands:
  - command: kubectl wait --for=delete db/single-node -n $NAMESPACE
  - command: kubectl wait --for=delete psmdb/single-node -n $NAMESPACE
collectors:
  - command: kubectl get db/single-node -n ${NAMESPACE} -o yaml
  - command: kubectl get psmdb/single-node -n ${NAMESPACE} -o yaml