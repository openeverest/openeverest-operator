apiVersion: kuttl.dev/v1
kind: TestStep
timeout: 600
commands:
  - script: |-
     set -euo pipefail

     kubectl delete pod/pg-client -n "${NAMESPACE}" --ignore-not-found=true
     kubectl run -n "${NAMESPACE}" pg-client --image=busybox --restart=Never --command -- sleep infinity
