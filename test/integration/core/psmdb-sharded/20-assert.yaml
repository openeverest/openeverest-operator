apiVersion: kuttl.dev/v1
kind: TestAssert
timeout: 30
collectors:
  - command: kubectl get dbc/test-psmdb-sharded-cluster -n ${NAMESPACE} -o yaml
  - command: kubectl get psmdb/test-psmdb-sharded-cluster -n ${NAMESPACE} -o yaml
  - command: kubectl get deploy/everest-controller-manager -n everest-system -o yaml
  - type: pod
    namespace: everest-system
    selector: control-plane=controller-manager
    tail: 100
resourceRefs:
  - apiVersion: everest.percona.com/v1alpha1
    kind: DatabaseCluster
    name: test-psmdb-sharded-cluster
    ref: db
  - apiVersion: psmdb.percona.com/v1
    kind: PerconaServerMongoDB
    name: test-psmdb-sharded-cluster
    ref: psmdb
assertAll:
  - celExpr: "has(psmdb.spec.sharding.mongos.configuration) && psmdb.spec.sharding.mongos.configuration != ''"
    message: "psmdb.spec.sharding.mongos.configuration is empty — proxy.config was not propagated"

  - celExpr: "psmdb.spec.sharding.mongos.configuration.contains('operationProfiling')"
    message: "psmdb.spec.sharding.mongos.configuration does not contain 'operationProfiling'"

  - celExpr: "psmdb.spec.sharding.mongos.configuration.contains('slowOp')"
    message: "psmdb.spec.sharding.mongos.configuration does not contain 'slowOp'"
---
apiVersion: everest.percona.com/v1alpha1
kind: DatabaseCluster
metadata:
  name: test-psmdb-sharded-cluster
spec:
  proxy:
    config: "operationProfiling:\n  mode: slowOp\n"
---
apiVersion: psmdb.percona.com/v1
kind: PerconaServerMongoDB
metadata:
  name: test-psmdb-sharded-cluster
spec:
  sharding:
    mongos:
      configuration: "operationProfiling:\n  mode: slowOp\n"
